package org.bobocode.hoverla.bring.bean;

/**
 * The BeanScope enum represents the scope of a bean in the application context.
 *
 */
public enum BeanScope {
  /**
   * Represents that there is only one instance of the bean in the application context.
   */
  SINGLETON
}

